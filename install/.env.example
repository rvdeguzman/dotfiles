# Optional environment configuration for setup scripts
# Copy this to .env if you want to customize the setup behavior

# Arch Linux settings
ARCH_PACKAGE_MANAGER=paru      # Options: paru, yay
ARCH_WM_CHOICE=hyprland         # Options: hyprland, i3, both

# macOS settings
MAC_INSTALL_TYPE=full           # Options: base, full

# Common tools
INSTALL_ATUIN=true
INSTALL_ZOXIDE=true
INSTALL_OHMYZSH=true
INSTALL_SDKMAN=false
