# kanagawa
BG='#181616'        # dragon_black_3 - main background
FG='#c5c9c5'        # dragon_white - main foreground
GREEN='#87a987'     # dragon_green
ORANGE='#b6927b'    # dragon_orange
RED='#c4746e'       # dragon_red
BLUE='#8ba4b0'      # dragon_blue_2
YELLOW='#c4b28a'    # dragon_yellow
GRAY='#9e9b93'      # dragon_gray_2
WHITE='#c5c9c5'     # dragon_white
BLACK='#181616'     # dragon_black_3
TEAL='#949fb5'     # dragon_teal
VIOLET='#8992a7'   # dragon_violet
ASH='#737c73'      # dragon_ash
AQUA='#8ea4a2'     # dragon_aqua

# gruvbox
# BG='#282828'      # dark background
# FG='#ebdbb2'      # light foreground
# GREEN='#b8bb26'   # bright green
# ORANGE='#fe8019'  # bright orange
# RED='#fb4934'     # bright red
# BLUE='#83a598'    # bright blue
# YELLOW='#fabd2f'  # bright yellow
# GRAY='#928374'    # gray
# WHITE='#fbf1c7'   # light cream
# BLACK='#1d2021'   # darker background


# rebind prefix
unbind C-b
set-option -g prefix C-a
bind C-a send-prefix

set-option -g allow-rename off
setw -g clock-mode-colour $GREEN

set -g base-index 1 

# open windows in same directory
bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"

# statusbar
set -g status on
set -g status-interval 1
set -g status-position bottom
set -g status-justify centre
set -g status-style "fg=$FG,bg=$BG"
set-option -g status-left-length 20
set-option -g status-left-style default
set-option -g status-left "#[fg=$GREEN,bright]#(whoami)#[default]"
set-option -g status-right-length 140
set-option -g status-right-style "fg=$RED"
set-window-option -g window-status-style "fg=$VIOLET,bg=default"
set-window-option -g window-status-current-style "fg=$ORANGE,bg=default"

set -as terminal-features ",*:RGB" 

set -g history-limit 50000
set -g display-time 2000

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
#
# # allows for mouse support
set -g mouse on
#
# # for quick notes
# bind-key h split-window -h "vim ~/notes/notes.md"
# preix + space to jump back to prev window
bind a last-window


# # Smart pane switching with awareness of Vim splits.
# # See: https://github.com/christoomey/vim-tmux-navigator
 is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
     | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?|fzf)(diff)?$'"
 bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
 bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
 bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
 bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
 tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
 if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
     "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
 if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
     "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

 bind-key -T copy-mode-vi 'C-h' select-pane -L
 bind-key -T copy-mode-vi 'C-j' select-pane -D
 bind-key -T copy-mode-vi 'C-k' select-pane -U
 bind-key -T copy-mode-vi 'C-l' select-pane -R
 bind-key -T copy-mode-vi 'C-\' select-pane -l
